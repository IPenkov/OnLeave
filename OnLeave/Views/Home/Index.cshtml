@model OnLeave.Models.UtilityBuildingModel[]

@{
    ViewBag.Title = "Index";
}

@*@section TopOffers{
    @{Html.RenderPartial("TopOffers");}
}*@

@section LeftSideBar{
    @Html.Action("Search", "Home", null) @*Html.RenderPartial("_SearchSection", new OnLeave.Models.SearchViewModel());}*@
}


<div id="home_content" class="box featured">
    <div class="box-heading"><span>Продукти</span></div>
    <div class="box-content">
        <div class="box-product">
            <ul>
                @for (int i = 0; i < Model.Count(); i++)
                {
                    <li class=@(i % 3 == 0 ? "first-in-line" : "")@(i % 3 == 2 ? "last-in-line" : "")>
                        <div class="inner-indent">
                            <div class="image2">
                                <a href="@Url.Action("GetUtilityBuilding", "Home", new { buildingId = Model[i].Id})">                                    
                                    <img 
                                         src="@Url.Action("GetPhoto", "Account", new { photoId = Model[i].PhotoIds.First(), width= 229, height = 179 })" @*width="229" height="179"*@
                                         alt="Hotel @Model[i].Name"/>
                                </a>
                            </div>
                            <div class="name">
                                <a class="maxheight-feat" style="height: 38px;" href="">
                                    @(Model[i].Description.Length > 10 ? Model[i].Description.Substring(0, 10) : Model[i].Description)
                                    @if (Model[i].Description.Length > 10)
                                    {<text>...</text>}
                                </a>
                            </div>
                        </div>
                    </li>
                }
            </ul>
        </div>
        <div class="clear"></div>
    </div>
</div>

@section Scripts {
    
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/autosize")
    @Scripts.Render("~/bundles/jquery-ui")
       
}
    
